/// PocketJoy V1.0 business constants.

/// Daily work hours (V1.0 fixed at 8, not user-configurable).
const dailyWorkHours = 8;

/// Coins needed to synthesize 1 gold bar.
const goldBarThreshold = 1000;

/// Minimum interval between coin drops (minutes).
const defaultIntervalMin = 15;

/// Maximum interval between coin drops (minutes).
const defaultIntervalMax = 300;

/// Maximum salary allowed.
const maxSalary = 10000000;

/// Minimum salary allowed.
const minSalary = 1;

/// Maximum workdays in a month.
const maxWorkdays = 31;

/// Minimum workdays in a month.
const minWorkdays = 1;

/// Maximum interval in minutes (24 hours).
const maxIntervalMinutes = 1440;

/// Minimum interval in minutes.
const minIntervalMinutes = 1;

/// Default audio volume coefficient (35% of system volume).
const defaultVolumeCoefficient = 0.35;

/// Animation queue max size (prevent backlog).
const animationQueueMaxSize = 2;

/// System time rollback threshold (seconds).
const timeRollbackThresholdSeconds = 60;
